const nombres = ['Francisco','Javier','Tejada','Lorenzo'];

console.log(nombres);

const numero_caracteres = nombres.map( nombre => `${nombre} tiene ${nombre.length} letras` );

console.log(numero_caracteres);

/* 
(parametro1, parametro2)=>{
    return
} */
